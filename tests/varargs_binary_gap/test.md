# varargs_binary_gap

The reduction has a fixed binary case and a three-or-more case that shrinks its input. Its binary pair word supports only int64 pairs. A mixed pair must fail at that word; it must not fall into a non-shrinking variadic retry.

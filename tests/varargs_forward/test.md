# varargs_forward

A facade and two library layers forward positional and named packs. Producers run exactly once in written order, private method homes and caller policy remain meaningful, and static fields plus bound instance identity survive forwarding.

The middle library function has multiline parameters, a multiline call, and a block with a local binding. These forms work in any function; main has no special body grammar.

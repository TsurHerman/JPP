# Named arguments across contexts

The same facade calls through two more modules to a named-only score method.
Two fresh programs select fallback and caller override. Declarations permute
named slot order; argument routing and context collapse must preserve the result.

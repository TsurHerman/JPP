# varargs_dispatch

The ratified decision table runs through source: fixed coverage, typed and predicate rest refinement, explicit pairwise order, and structural shape tie-breaking. No tail ranks are summed.

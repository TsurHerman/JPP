# the PLAIN world: no override. report(x) = double(x) = x + x.
using ints
using stats
using algebra
using Base.Test

main() = {
    check("plain: report(21) = 21+21", report(21), 42)
    check("plain: square(21) = 21*21", square(21), 441)
}

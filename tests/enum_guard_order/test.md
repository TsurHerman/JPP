# Error messages receive more urgent review

Both errors and warnings need attention, but errors need immediate review.
The source explicitly orders `isError` below `needsAttention` through the
existing type-only order word, using the predicates' word identities.

The label chooses the authored refinement, the review function demonstrates
comma-separated conjunction, and the caption reverses the order of named
arguments between definitions. Predicate ordering remains per argument and
pairwise; no implicit transitive closure or arbitrary logical proof is added.

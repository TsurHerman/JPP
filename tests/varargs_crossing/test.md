# varargs_crossing

At two int64 arguments, one method has a better fixed first coordinate but a weaker unconstrained rest coordinate. The other constrains both rest elements to int64. Neither dominates pointwise; a narrower arity shape must not compensate for the crossing.

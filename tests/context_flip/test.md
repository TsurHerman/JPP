# context_flip

**Validates (README §1, §9):** ordered context — position breaks ties,
and flipping the order flips the winner. Two programs, identical
modules, opposite `using` order: `first_leads` gets 1, `second_leads`
gets 2. Equal specificity everywhere; nothing but position differs.

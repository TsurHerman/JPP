# private_helpers

Private words are usable inside their declaring module, cannot be overridden by a caller's same-spelled word, and are not exported through folder aggregation. Predicates used internally follow the same rule.

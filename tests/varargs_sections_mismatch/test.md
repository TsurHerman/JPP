# varargs_sections_mismatch

One T is shared by positional and named uniform rests. An int64 positional element and a float64 named element must fail binding even though each section separately has one element.

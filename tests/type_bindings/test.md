# type_bindings

A where-bound type can be used as a value in the body. Explicit type inputs in equality gates bind their values, not the common kind `type`. Mutual authored edges are queried directly, without closure.

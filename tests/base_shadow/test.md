# base_shadow

A source-tree `Base.jpp` replaces the library's `Base/Base.jpp`. Implicit
imports resolve that ordinary module in the application and its libraries.
The foundation itself does not implicitly import itself.

# the mirror program: same modules, opposite edge, opposite winner.
# nothing about the two gates changed — only the authored order did.
using signed_below
using rules
using Test

main() = {
    check("the opposite edge flips the winner", g(1), 2)
}

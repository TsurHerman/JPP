# Export declares a tunnel

The library declares inspect with no candidate body. Calls bind to a real caller
implementation, and both modules refer to the same word identity. An unused
requirement needs no implementation. Export does not claim universal coverage.

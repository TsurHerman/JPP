# varargs

Statically shaped positional and named rests preserve data and static fields; splats invert capture without cross-fill. Uniform T requires one identity, while short predicate annotation checks each element independently, including vacuous empty capture.

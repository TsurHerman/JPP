# unused_input

A fresh, unannotated named input that is ignored is a declaration error on
an ordinary word too; an unused method is still checked. Explicitly typed
or predicate-annotated inputs are permitted to be unused (`unused_ground`).

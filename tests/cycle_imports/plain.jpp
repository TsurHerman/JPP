using a
using Base.Test

main() = {
    check("three-file cycle executes through original homes", alpha(), 42)
    check("importing one member exposes every public unit word", gamma(), 42)
    check("first private helper retains file identity", left(), 11)
    check("second private helper retains file identity", right(), 22)
}

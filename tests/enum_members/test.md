# Enum members are defined static values

Projection from a known enum type selects an existing member of that enum.
Type-returning calls and local aliases use the same projection. Members retain
their owning enum type and static value through ordinary helper calls.

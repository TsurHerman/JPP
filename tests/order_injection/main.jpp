# VALIDATES: injecting one order fact cures a gated-vs-gated clash, in
# the same module that raised it.
using clash
using Base.Test

main() = {
    check("injected edge cures the clash", category(1), 2)
}

# unused_untyped_ground

A fresh input with no annotation or use is rejected. A string spelling
its name, or a longer identifier containing it, does not count as a use.
This is the unannotated counterpart of the valid `unused_ground` case.

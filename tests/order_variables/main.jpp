# VALIDATES: `<:` takes variables over classes, and specificity inside
# the order word picks the specific fact over the general rule.
using order
using rules
using Base.Test

main() = {
    check("specific fact beats general rule", category(1), 2)
}

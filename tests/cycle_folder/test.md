# cycle_folder

Folder-to-child edges participate in import components. A child importing its
own folder forms a unit with that folder; its exported methods appear once,
its private helper retains its source home, and other folder siblings remain
publicly available. Generated STATIC constants must remain finite.

A second program imports a folder containing just one member of a unit
spanning two folders. The entire public unit remains visible and deduplicated.

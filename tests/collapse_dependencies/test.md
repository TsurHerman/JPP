# collapse_dependencies

With A<B and B<C but no A<C, all three candidates leave A maximal. Removing the losing B method would introduce ambiguity. Context collapse must retain decision dependencies, including losing candidates.

# caller_context

**Validates (README §1):** dispatch is caller-derived. `algebra`
imports nothing; `+` means what THIS context says, and the same
generic `double` serves every domain the context grounds. Two checks:
the meaning arrives, and it generalizes.

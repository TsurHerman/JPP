# One generic serves integer and floating domains. Algebra now implicitly
# imports Base, while these explicit arithmetic providers remain ahead of it.
using ints
using floats
using algebra
using Base.Test

main() = {
    check("caller supplies meaning", double(21), 42)
    check("one generic, another domain", double(1.5), 3.0)
}

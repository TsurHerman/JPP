# One generic double function uses the caller's explicit arithmetic providers.
using ints
using floats
using algebra
using Base.Test

main() = {
    check("caller supplies meaning", double(21), 42)
    check("one generic, another domain", double(1.5), 3.0)
}

# varargs_predicate_reject

A short predicate rest checks every captured element independently. One boolean among otherwise numeric inputs makes the candidate inapplicable; accepting the first element cannot certify the whole tail.

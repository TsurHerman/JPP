# varargs_scale

A shrinking sum has explicit empty and singleton identities, fixed binary addition, and a three-or-more reduction. Separate root programs exercise 0, 1, 2, 8, and 32 elements through two library layers. Each result is checked against the arithmetic sum. This is a compile-cost sample as well as a correctness case.

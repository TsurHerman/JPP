using Base.Zig

NativeOrder = Zig.std.math.Order
DatabaseOrder = zig{ enum { lt, eq, gt } }

isEarlier(NativeOrder.lt) = true

# A database's separately declared order is not the native comparison enum.
main() = isEarlier(DatabaseOrder.lt)

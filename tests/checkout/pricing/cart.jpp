using arithmetic
using model.basket
using model.line
using model.priced
using pricing.lines
export priceBasket

priceBasket(bag) = pricePair(firstItem(bag), secondItem(bag))
pricePair(a<:Line, b<:Line) = pricedCart(grossLine(a) + grossLine(b), netLine(a) + netLine(b), gramsLine(a) + gramsLine(b))

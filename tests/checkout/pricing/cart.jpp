using model.basket
using model.line
using model.priced
using pricing.lines
export priceBasket

priceBasket(bag) = {
    a = firstItem(bag)
    b = secondItem(bag)
    pricedCart(grossLine(a) + grossLine(b), netLine(a) + netLine(b), gramsLine(a) + gramsLine(b))
}

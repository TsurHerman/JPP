using model.line
export basket, firstItem, secondItem

# The two-line public contract remains; storage is now an ordinary tuple.
basket(a::T, b::T) where T <: Line = (a, b)
firstItem(bag) = bag.0
secondItem(bag) = bag.1

export choose, atLeast, isZero

choose(condition::bool, yes::T, no::T)::T where T = zig{ if (condition) yes else no }

atLeast(a::int64, b::int64)::bool = zig{ a >= b }

isZero(n::int64)::bool = zig{ n == 0 }

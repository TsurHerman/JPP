# any_order_gap

Locally defining the ordinary always-true predicate Any does not give it
a special rank or imply any ordering facts. Both it and Integer accept an
integer; without an authored edge, their methods in one module are
ambiguous. The unimported Base module must not supply a hidden edge.

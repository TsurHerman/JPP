# dispatch

**Validates (README §1, §4):** dispatch selects the method by argument
type, and exact beats bare. Every method returns its own number, so
the returned value IS the identity of the method that ran — nothing
hidden.

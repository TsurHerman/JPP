# main.jpp — scratch entry point.

using reals

main() = {
    const a = 5;              # comptime int literal
    const b = 5.5;            # comptime float literal
    var c = a + b             # materializes: float64
    var d = c
    println(typeof(c))        # io module TBD; builtin for now
}
